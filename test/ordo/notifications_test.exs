defmodule Ordo.NotificationsTest do
  use Ordo.DataCase, async: false

  # Think about set of simple tests
  # For Production app it wouldn't be skipped, but I'm not sure about
  # current implementation of Notifications yet, so I decided to leave it
  # alone for now
end
